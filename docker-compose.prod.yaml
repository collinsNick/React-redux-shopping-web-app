version: "3.8"

services:
  react-duka-prod:
    container_name: react-duka-prod
    build:
      context: .
      dockerfile: docker/Dockerfile.prod
    ports:
      - "8877:80"
    stdin_open: true
